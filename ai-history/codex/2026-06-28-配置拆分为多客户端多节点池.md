# 2026-06-28 - 配置拆分为多客户端多节点池

元信息：
- Agent：Codex
- 创建时间：2026-06-28 01:45:54 Asia/Shanghai
- 最近更新时间：2026-06-28 01:45:54 Asia/Shanghai
- 上下文版本：0.1.6
- 项目版本：TBD
- 资料来源：CHANGELOG、git status、当前项目文件、当前对话

## 历史会话摘要与任务摘要

本轮对 `/Users/Shared/Shared_AI_Workspace/SharedProjects/VPN` 做增量上下文维护。上一轮项目资料维护锚点为 2026-06-15 的 0.1.5 文档，本轮覆盖到 2026-06-28 当前工作树状态。

本轮没有修改 VPN 配置逻辑，只更新长期上下文文档、交接记录和 ai-history 索引。

## 上次项目资料维护后的新增变化

### 变化 1：主配置拆分为四份 `r_equ_*` 配置

#### 目的

把原先按客户端维护的主配置，进一步拆分为 Mac / Android 与仅美国优先 / 全地区赠送节点两类使用场景，便于按设备和节点池选择导入。

#### 做出的决定

- `r_equ_onlyUS_mac` 作为 Shadowrocket / Mac 仅美国优先配置。
- `r_equ_all_countries_mac` 作为 Shadowrocket / Mac 全地区赠送节点配置。
- `r_equ_onlyUS_android` 作为 Mihomo / Clash Meta for Android 仅美国优先配置。
- `r_equ_all_countries_android` 作为 Mihomo / Clash Meta for Android 全地区赠送节点配置。
- 旧 `local_group.conf` 和 `isp_local` 当前在 Git 工作树中显示为 deleted；提交前需要用户确认这是预期迁移。

#### 发现的问题

旧文档仍把 `local_group.conf` 和 `isp_local` 描述为当前主配置，已经无法解释当前工作树中四份 `r_equ_*` 配置和旧文件删除状态。

#### 解决方案

更新 `AGENTS.md`、`README.md`、`docs/PROJECT_CONTEXT.md`、`docs/TESTING.md` 和 `docs/HANDOFF.md`，将当前配置矩阵写入项目长期上下文。

#### 涉及文件

- `r_equ_onlyUS_mac`
- `r_equ_all_countries_mac`
- `r_equ_onlyUS_android`
- `r_equ_all_countries_android`
- `AGENTS.md`
- `README.md`
- `CHANGELOG.md`
- `docs/PROJECT_CONTEXT.md`
- `docs/TESTING.md`
- `docs/HANDOFF.md`

#### 验证情况

- 已确认：`git status --short` 显示 `local_group.conf` 和 `isp_local` 为 deleted，四份 `r_equ_*` 配置为 untracked。
- 已确认：`r_equ_onlyUS_mac` 为 229 行，`r_equ_all_countries_mac` 为 225 行。
- 已确认：`r_equ_onlyUS_android` 为 686 行，`r_equ_all_countries_android` 为 639 行。
- 已确认：两份 Mac 配置都有 `[General]`、`[Proxy Group]`、`[Rule]`、`[Host]`、`[URL Rewrite]`、`[MITM]` 节。
- 已确认：两份 Android 配置都有 `sniffer`、`tun`、`dns`、`proxy-providers`、`proxy-groups`、`rule-providers`、`rules` 顶层结构。

#### 未验证 / 风险

- 未运行 Shadowrocket / Mac 或 Mihomo Android 客户端导入验证。
- 未验证四份配置的真实规则命中、订阅刷新、远程规则刷新、DNS leak、TUN 或 fake-ip 行为。
- 旧文件删除和新文件新增尚未提交，迁移是否完成需要用户确认。

### 变化 2：`赠送节点` 成为全地区配置的高流量和兜底策略

#### 目的

给不只使用美国节点的场景提供全地区赠送节点池，覆盖指定国家和地区的非静态赠送节点。

#### 做出的决定

- 全地区 Mac 配置使用 `赠送节点`，`FINAL` 指向 `赠送节点`。
- 全地区 Android 配置使用 `赠送节点`，`MATCH` 指向 `赠送节点`。
- 仅美国优先配置继续使用 `赠送美国`、`赠送美国主选` 和 `赠送非美兜底`。

#### 发现的问题

旧文档只描述 `赠送美国`，没有描述 `赠送节点` 与全地区配置之间的关系。

#### 解决方案

在 README、PROJECT_CONTEXT、TESTING 和 HANDOFF 中补充 `赠送节点` 的职责、验证项和风险边界。

#### 涉及文件

- `r_equ_all_countries_mac`
- `r_equ_all_countries_android`
- `README.md`
- `docs/PROJECT_CONTEXT.md`
- `docs/TESTING.md`
- `docs/HANDOFF.md`

#### 验证情况

- 已确认：`r_equ_all_countries_mac` 中 `FINAL` 指向 `赠送节点`。
- 已确认：`r_equ_all_countries_android` 中 `MATCH` 指向 `赠送节点`。
- 已确认：全地区 Android 配置中 `赠送节点` 使用非静态节点筛选。

#### 未验证 / 风险

- 未验证 `赠送节点` 是否在真实客户端中按预期选择节点。
- 未验证全地区赠送节点池是否会误纳入不适合高流量服务的节点。

### 变化 3：运行态文件移入 `logs/` 边界

#### 目的

记录运行态日志和 SQLite 文件的位置变化，避免未来 agent 误读或误提交。

#### 做出的决定

- `logs/` 下的 Clash 日志、SQLite 文件和 `.DS_Store` 只记录存在性和大小。
- 根目录 `.DS_Store` 是未跟踪系统元数据文件，不应提交。
- `.gitignore` 当前只观察到 `LOGS/`，是否需要补充小写 `logs/` 和 `.DS_Store` 需要用户确认后再改。

#### 发现的问题

旧文档记录运行态文件位于根目录；当前文件已经在 `logs/` 目录下，且根目录还有 `.DS_Store` 未跟踪文件。

#### 解决方案

更新文档中的数据安全边界和后续事项。

#### 涉及文件

- `.gitignore`
- `logs/`
- `.DS_Store`
- `AGENTS.md`
- `README.md`
- `docs/PROJECT_CONTEXT.md`
- `docs/TESTING.md`
- `docs/HANDOFF.md`

#### 验证情况

- 已确认：本轮只通过 `ls -lh` 查看运行态文件大小，未读取内容。
- 已确认：`logs/` 下存在 Clash 日志、SQLite 数据库文件和 `.DS_Store`。

#### 未验证 / 风险

- 未确认 `.gitignore` 是否足以在当前文件系统和 Git 配置下稳定忽略 `logs/`。
- 未清理或删除 `.DS_Store`，因为本轮只维护文档，不做删除操作。

## 跨会话补充

本轮没有导入完整原始对话。内容根据现有 CHANGELOG、git status、当前项目文件和当前对话重建。

## 遇到的问题

- 未发现可确认的配置逻辑 bug。
- 项目当前没有业务代码脚本，因此本轮没有触发脚本注释维护流程，也没有读取 `r-coding-guidelines`。
- skill 要求最终提供一键 Git 归档命令，但不要求 agent 自己执行 commit / push；本轮未自动提交或推送。

## 输出结果

- 更新长期上下文文档。
- 更新 `ai-history/INDEX.md`。
- 新增 `ai-history/codex/2026-06-28-配置拆分为多客户端多节点池.md`。

## 后续事项

- 用户确认后，决定是否提交旧 `local_group.conf` / `isp_local` 删除和四份 `r_equ_*` 新配置。
- 真机验证四份 `r_equ_*` 配置的导入、订阅刷新、远程规则刷新和规则命中。
- 用户确认后，可补充 `.gitignore` 排除 `.DS_Store` 和运行态目录。

## 原始归档

本文件未导入完整原始对话，内容根据 CHANGELOG、git status、当前项目文件和当前对话重建。
